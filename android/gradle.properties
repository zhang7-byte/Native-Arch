org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# file_picker 11.0.2 skips its kotlinOptions{jvmTarget} on AGP 9, so its Kotlin
# would default to a target that mismatches its Java 17 compileOptions. Relax the
# check to a warning (the dexer normalises both targets for Android anyway).
kotlin.jvm.target.validation.mode=warning
